import React from 'react';
import FormHistory from './FormHistory/FormHistory.component';

export default function History (): React.JSX.Element {
  return (
        <div>
            <FormHistory></FormHistory>
        </div>
    );
}
